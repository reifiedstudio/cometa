{
  "name": "@cometa/stream-router",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target node --format cjs"
  },
  "dependencies": {
    "@aws-sdk/client-sqs": "^3.700.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145"
  }
}
